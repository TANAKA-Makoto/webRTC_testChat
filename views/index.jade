extends layout

block content
  h1= title
  p Welcome to #{title}
  nav(id="links")
    ul
    - for val, index in child
        li: a(href="#{val}") #{pageTitle[index]}
        br
  p if you use chrome, please set a flag in "chrome://flags/"